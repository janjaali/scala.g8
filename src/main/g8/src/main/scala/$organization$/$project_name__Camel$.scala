package $organization$

import com.typesafe.scalalogging.LazyLogging

object $project_name;format="Camel"$ extends LazyLogging {
  def main(args: Array[String]): Unit = {
    logger.info("$project_name$ started ...")
  }
}
